# Nginx configuration for silkshop.online
# Single domain with path-based routing
# Place this file at /etc/nginx/sites-available/silkshop

# Upstream for backend API
upstream backend {
    server localhost:3000;
}

# Upstream for frontend admin
upstream frontend-admin {
    server localhost:5173;
}

# Upstream for frontend client
upstream frontend-client {
    server localhost:5174;
}

# Main server - silkshop.online
server {
    listen 80;
    server_name silkshop.online www.silkshop.online;

    # Backend API - /api/*
    location /api {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
        
        # Rewrite /api to root for backend
        rewrite ^/api/?(.*) /$1 break;
    }

    # Admin Panel - /admin/*
    location /admin {
        proxy_pass http://frontend-admin;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Frontend Client - root and all other paths
    location / {
        proxy_pass http://frontend-client;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS Configuration (uncomment after setting up SSL with Certbot)
# Certbot will auto-generate these, but here's the template:

# server {
#     listen 443 ssl http2;
#     server_name silkshop.online www.silkshop.online;
# 
#     ssl_certificate /etc/letsencrypt/live/silkshop.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/silkshop.online/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
# 
#     # Backend API - /api/*
#     location /api {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 75s;
#         rewrite ^/api/?(.*) /$1 break;
#     }
# 
#     # Admin Panel - /admin/*
#     location /admin {
#         proxy_pass http://frontend-admin;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# 
#     # Frontend Client - root and all other paths
#     location / {
#         proxy_pass http://frontend-client;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
